import React from 'react';
import ReactDOM from 'react-dom';
import { default as PokemonBackup } from "./components/PokemonBackup/PokemonBackup";

ReactDOM.render(<PokemonBackup />, document.getElementById('pokemon-app'));